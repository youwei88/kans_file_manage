package common.util;

public class SapConn {/*

	static Properties p = new Properties();

	public static Client getConn() {
		Client client = null;
		try {
			p.load(SapConn.class.getResourceAsStream("../../../config.properties"));

			client = JCO.createClient(p.getProperty("SAP.CLIENT"), // SAP client
					p.getProperty("SAP.USER_ID"), // userid
					p.getProperty("SAP.PASSWORD"), // password
					p.getProperty("SAP.LANGUAGE"), // language (null for the
													// default language)
					p.getProperty("SAP.HOST"), // application server host name
					p.getProperty("SAP.SYSTEM_NUMBER")); // system number
			client.connect();
		} catch (Exception e) {
			e.printStackTrace();
		}
		return client;
	}

	public static Function getFunction(Client client, String apiName) {
		Repository repository = new Repository("kansRepository", client);
		IFunctionTemplate iFunctionTemplate = repository.getFunctionTemplate(apiName);
		Function function = iFunctionTemplate.getFunction();
		return function;
	}

*/}
